<div>
  <% if User.new.errors[field].present?  %>
    <ul id="list-unstyled">
        <% User.new.errors[field].each do |error| %>
            <li class="red "><%= error %></li>
        <% end %>
    </ul>
  <% end %>
</div>
